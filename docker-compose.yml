services:
  backend:
    build:
      context: ./backend
    container_name: hotelcast-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
